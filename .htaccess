<IfModule mod_rewrite.c>

    RewriteEngine On

    # Jangan proses ulang request yang sudah berada di /public/
    RewriteCond %{REQUEST_URI} !^/public/

    # Arahkan semua request ke folder public
    RewriteRule ^(.*)$ public/$1 [L]

</IfModule>